const request = require("request");
const oauth = require("OAuth");

const OAuth = new oauth.OAuth("REQUEST URL", "ACCESS_URL", "CONSUMER_KEY", "CONSUMER_SECRET", "1.0A", null, "HMAC-SHA1");

function getTweets(){



}
